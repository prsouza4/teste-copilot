# API Contract Reference

> Auto-generated by the api-docs agent. Do not edit manually.  
> Last updated: 2026-03-11 22:22:47 UTC

Este diretório contém a documentação consolidada de todos os endpoints das APIs do projeto, gerada automaticamente pelo agente `api-docs` sempre que um endpoint é criado ou modificado.

## Como usar

- **Frontend**: Antes de implementar qualquer UI que consome uma API, verifique se existe o arquivo correspondente aqui. Se existir, use-o como fonte de verdade — não leia o código .NET diretamente.
- **Backend**: Após criar ou modificar um endpoint, o agente `api-docs` é acionado automaticamente via PR para gerar/atualizar a documentação.

## Estrutura

```
docs/api/
├── README.md              ← este arquivo (índice geral)
├── users/
│   ├── post-create-user.md
│   ├── get-user-by-id.md
│   └── get-list-users.md
├── orders/
│   ├── post-create-order.md
│   └── get-order-by-id.md
└── payments/
    └── post-process-payment.md
```

## Formato de cada arquivo

Cada arquivo de endpoint contém:
- Rota, método HTTP e autenticação necessária
- Schema de request e response (JSON)
- Regras de validação
- Exemplos prontos: `curl`, `fetch` e TypeScript

---

## Índice de Endpoints

### Users

User registration and management endpoints.

| Method | Endpoint | Description | Doc |
|--------|----------|-------------|-----|
| POST | `/api/users` | Create a new user | [→](users/post-create-user.md) |
| GET | `/api/users/{id}` | Get a user by ID | [→](users/get-user-by-id.md) |
| GET | `/api/users` | List users with pagination | [→](users/get-list-users.md) |

---

## Common Patterns

### Pagination

Endpoints returning lists use this pagination format:

```typescript
interface PaginatedResponse<T> {
  items: T[]
  page: number
  pageSize: number
  totalCount: number
  totalPages: number
}
```

**Query parameters:**
- `page` (int, default: 1) — Page number (1-based)
- `pageSize` (int, default: 10) — Items per page

### Error Responses

All endpoints follow RFC 7807 (Problem Details) for errors:

```typescript
interface ProblemDetails {
  type: string
  title: string
  status: number
  errors?: Record<string, string[]>
  detail?: string
}
```

### Date Formats

All timestamps use ISO 8601 format in UTC:
```
2024-01-15T10:30:00Z
```

---

## Changelog

### 2026-03-11
- [CREATED] POST /api/users - Create user endpoint
- [CREATED] GET /api/users/{id} - Get user by ID endpoint  
- [CREATED] GET /api/users - List users endpoint
- Initial documentation for User Registration microservice
