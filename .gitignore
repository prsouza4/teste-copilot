# Logs — gerados pelos session hooks
logs/

# Work folder — arquivos locais de rastreamento (não commitar)
work/

# MCP Servers — dependências locais (não commitar)
mcp-servers/**/node_modules/
# mcp-servers/**/package-lock.json  ← lock files devem ser commitados

# .NET
bin/
obj/
*.user
.vs/

# Node.js
node_modules/
.next/
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
